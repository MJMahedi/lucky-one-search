import React from 'react';

const Cart = (props) => {
    
    // const {name , handleButton }= props;
    console.log(props.cart);

    return (
        <div>
            <h2>Cart Section </h2>
            {/* <h2>Hello {name}</h2> */}
        </div>
    );
};

export default Cart;